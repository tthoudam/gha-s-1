name: Terraform Commands
on:
  workflow_call:
    inputs:
      command:
        required: true
        type: string
jobs:
  workflow_call:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Terraform Action
      run: terraform ${{ inputs.command }} -auto-approve -input=false
